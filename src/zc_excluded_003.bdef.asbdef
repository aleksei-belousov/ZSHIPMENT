projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_EXCLUDED_003 alias Excluded
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}